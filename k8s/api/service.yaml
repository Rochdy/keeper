apiVersion: v1
kind: Service
metadata:
  name: grade-submission-api
  namespace: default
spec:
  selector:
    app: grade-submission-api
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30001
