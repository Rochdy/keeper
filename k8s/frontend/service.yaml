apiVersion: v1
kind: Service
metadata:
  name: grade-submission-frontend
  namespace: default
spec:
  selector:
    app: grade-submission-frontend
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30002
